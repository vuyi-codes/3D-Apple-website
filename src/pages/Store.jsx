// Store Shell page — Step 6 will build this out fully.
// Product card grid, filter sidebar, and a Zustand-powered cart drawer.
// Frontend-only — no real checkout, all mock data.
import Footer from "../components/Footer";

const Store = () => {
    return (
        <>
            <main className="pt-16 min-h-screen bg-black text-white flex-center flex-col gap-8">
                <h1 className="text-4xl font-semibold">Store</h1>
                <p className="text-dark-100 text-lg">Coming soon — built in Step 6.</p>
            </main>
            <Footer />
        </>
    );
};

export default Store;
