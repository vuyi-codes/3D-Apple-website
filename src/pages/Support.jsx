// Support page — Step 8 will build this out fully.
// FAQ accordion with search filter, topic cards — all mock data.
import Footer from "../components/Footer";

const Support = () => {
    return (
        <>
            <main className="pt-16 min-h-screen bg-black text-white flex-center flex-col gap-8">
                <h1 className="text-4xl font-semibold">Support</h1>
                <p className="text-dark-100 text-lg">Coming soon — built in Step 8.</p>
            </main>
            <Footer />
        </>
    );
};

export default Support;
